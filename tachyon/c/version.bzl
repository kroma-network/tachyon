# See https://semver.org/
VERSION_MAJOR = 0
VERSION_MINOR = 3
VERSION_PATCH = 1
VERSION_PRERELEASE = ""

VERSION = ".".join([
    str(VERSION_MAJOR),
    str(VERSION_MINOR),
    str(VERSION_PATCH),
])
