# See https://semver.org/
VERSION_MAJOR = 0
VERSION_MINOR = 2
VERSION_PATCH = 0
VERSION_PRERELEASE = ""

VERSION = ".".join([
    str(VERSION_MAJOR),
    str(VERSION_MINOR),
    str(VERSION_PATCH),
])
